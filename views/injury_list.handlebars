{{> navbar}}
<div class="topBox">
  <a class="btn btn-primary" href="/" role="button" id="back-btn">Back</a>
  <h2 class="titleName"> Injury List </h2>
</div>

<div>
  <button type="button" name="filter" class="btn btn-primary">Filter</button>
</div>

{{#each sports}}
  <div class="responsive">
    <a href="/common-injuries">
    <div class="card">
        <img src={{imageURL}} alt={{name}}>
        <h6 class="name">{{name}}</h6>
    </div>
    </a>
  </div>
{{/each}}
